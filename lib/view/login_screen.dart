import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // Isi dengan tampilan halaman berikutnya setelah splash screen
    );
  }
}